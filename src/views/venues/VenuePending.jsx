import React from 'react';
import VenueBookings from './VenueBookings';

const VenuePending = () => {
    return <VenueBookings initialTab="Pending" hideTabs={true} />;
};

export default VenuePending;
