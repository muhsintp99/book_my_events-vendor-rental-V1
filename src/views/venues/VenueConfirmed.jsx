import React from 'react';
import VenueBookings from './VenueBookings';

const VenueConfirmed = () => {
    return <VenueBookings initialTab="Confirmed" hideTabs={true} />;
};

export default VenueConfirmed;
