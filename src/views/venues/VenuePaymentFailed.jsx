import React from 'react';
import VenueBookings from './VenueBookings';

const VenuePaymentFailed = () => {
    return <VenueBookings initialTab="Payment Failed" hideTabs={true} />;
};

export default VenuePaymentFailed;
