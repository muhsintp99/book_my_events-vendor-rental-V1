import React from 'react';
import VenueBookings from './VenueBookings';

const VenueCancelled = () => {
    return <VenueBookings initialTab="Cancelled" hideTabs={true} />;
};

export default VenueCancelled;
