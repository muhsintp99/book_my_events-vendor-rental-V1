import React from 'react';
import Floristbookings from './Floristbookings';

const Floristcancelled = () => {
    return <Floristbookings initialTab="Cancelled" hideTabs={true} />;
};

export default Floristcancelled;
