import React from 'react';
import Lightbookings from './Lightbookings';

const Lightpending = () => {
    return <Lightbookings initialTab="Pending" hideTabs={true} />;
};

export default Lightpending;
