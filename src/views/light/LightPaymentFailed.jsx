import React from 'react';
import Lightbookings from './Lightbookings';

const LightPaymentFailed = () => {
    return <Lightbookings initialTab="Payment Failed" hideTabs={true} />;
};

export default LightPaymentFailed;
